# Phase 1.5a — Классы персонажей: Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add 5 purchasable character classes (Медик / Танк / Спецназ / Двойник / Бог) with unique abilities, fractional lives support for armor mechanics, and class-select flow between profile-select and game start.

**Architecture:** Extend existing GameEngine with a class-aware damage pipeline (`resolveDamage` helper consuming `classState`), add new action types (`use-ability` for lightning, `knife` as ItemId), introduce new screen `ClassSelectScreen`, migrate `PlayerProfile.ownedClasses[]`. All changes are backward-compatible with classless play.

**Tech Stack:** Same as Phase 1 — TypeScript strict, Three.js, Vite, vitest, Playwright. No new dependencies.

**Specification:** [docs/superpowers/specs/2026-05-04-phase-1.5a-classes-design.md](../specs/2026-05-04-phase-1.5a-classes-design.md)

---

## Conventions

- TDD for all engine and bot changes. UI tested via Playwright e2e at the end.
- Russian commit messages (`feat: добавить ...`).
- DOM via `createElement`/`textContent`/`appendChild` — never `innerHTML`.
- All paths relative to `/Users/oxi/cc/oxi/life-roulette/`.
- Run from main branch (or on a fresh feature branch — task runner will create a worktree).

## File Structure

```
life-roulette/
├── src/
│   ├── game/
│   │   ├── types.ts            ← MODIFY: ClassId, ClassState, Player, Action, GameEvent, PlayerView
│   │   ├── classes.ts          ← NEW: class definitions + factory for classState
│   │   ├── damage.ts           ← NEW: resolveDamage(target, baseDamage, source) helper
│   │   ├── engine.ts           ← MODIFY: shoot uses damage.ts, use-item handles knife and chocolate-medic, new use-ability case, load-chamber resets per-chamber state
│   │   ├── bot.ts              ← MODIFY: lightning + knife branches
│   │   └── views.ts            ← MODIFY: include classId in otherPlayers
│   ├── persistence/
│   │   └── profiles.ts         ← MODIFY: ownedClasses field with migration
│   ├── menu/
│   │   ├── ShopScreen.ts       ← MODIFY: class purchase section
│   │   └── ClassSelectScreen.ts ← NEW
│   ├── hud/
│   │   ├── PlayerListHud.ts    ← MODIFY: class icon + fractional hearts
│   │   ├── ItemBarHud.ts       ← MODIFY: knife icon + lightning button
│   │   └── PrivatePeekScreen.ts ← (no changes)
│   ├── controllers/
│   │   └── OfflineGameController.ts ← MODIFY: class select flow + use-ability dispatch
│   ├── i18n.ts                 ← MODIFY: class names + descriptions
│   └── controllers/start-game.ts ← MODIFY: integrate ClassSelectScreen
└── tests/
    └── unit/
        ├── classes.test.ts     ← NEW: class factory + per-class state defaults
        ├── damage.test.ts      ← NEW: resolveDamage unit tests
        ├── engine.test.ts      ← MODIFY: existing tests + new class scenarios
        ├── bot.test.ts         ← MODIFY: new branches
        ├── views.test.ts       ← MODIFY: classId exposure
        └── persistence/profiles.test.ts ← MODIFY: ownedClasses migration
```

---

## Phase A — Foundation: types, classes, persistence

### Task A.1: Доменные типы для классов

**Files:**
- Modify: `src/game/types.ts`

- [ ] **Step 1:** Добавить в `types.ts` после `type ItemId`:

```typescript
export type ItemId = 'chocolate' | 'magnifier' | 'knife';
export type ClassId = 'medic' | 'tank' | 'specops' | 'double' | 'god';
export type AbilityId = 'lightning';

export interface ClassState {
  // Tank
  tankBlockUsed: boolean;
  // Specops
  armorActive: boolean;
  armorChargesLeft: number;
  armorRoundsLeft: number;
  // Double
  knifeArmed: boolean;
  knifeUsed: boolean;
  // God
  lightningUsedThisChamber: boolean;
  lightningTotalUsed: number;
}
```

- [ ] **Step 2:** Обновить `Player`:

```typescript
export interface Player {
  id: string;
  name: string;
  profileId: string | null;
  lives: number;            // ИЗМЕНЕНО: может быть дробным с шагом 0.5
  inventory: Record<ItemId, number>;
  isBot: boolean;
  eliminated: boolean;
  classId: ClassId | null;  // НОВОЕ
  classState: ClassState;   // НОВОЕ
}
```

- [ ] **Step 3:** Расширить `Action`:

```typescript
export type Action =
  | { type: 'spin-roulette' }
  | { type: 'load-chamber' }
  | { type: 'use-item'; itemId: ItemId }
  | { type: 'use-ability'; ability: AbilityId; targetId: string }   // НОВОЕ
  | { type: 'shoot'; targetId: string };
```

- [ ] **Step 4:** Расширить `GameEvent`:

```typescript
export type GameEvent =
  | { type: 'roulette-spun'; firstPlayerId: string }
  | { type: 'chamber-loaded'; liveCount: number; blankCount: number }
  | { type: 'item-used'; playerId: string; itemId: ItemId }
  | { type: 'shot-fired'; shooterId: string; targetId: string; bullet: Bullet }
  | { type: 'lives-changed'; playerId: string; newLives: number }
  | { type: 'extra-turn-granted'; playerId: string }
  | { type: 'extra-turn-cap-hit'; playerId: string }
  | { type: 'player-eliminated'; playerId: string }
  | { type: 'turn-changed'; nextPlayerId: string }
  | { type: 'chamber-empty' }
  | { type: 'game-over'; winnerId: string }
  // НОВЫЕ:
  | { type: 'tank-block-triggered'; playerId: string }
  | { type: 'armor-broke'; playerId: string }
  | { type: 'knife-armed'; playerId: string }
  | { type: 'knife-doubled-damage'; playerId: string }
  | { type: 'lightning-cast'; casterId: string; targetId: string; damage: number };
```

- [ ] **Step 5:** Расширить `PlayerView.otherPlayers`:

```typescript
export interface PlayerView {
  selfPlayer: Player;
  otherPlayers: ReadonlyArray<{
    id: string;
    name: string;
    lives: number;
    eliminated: boolean;
    classId: ClassId | null;   // НОВОЕ
  }>;
  chamberLive: number;
  chamberBlank: number;
  currentPlayerId: string;
  phase: GamePhase;
}
```

- [ ] **Step 6:** Typecheck

```bash
cd /Users/oxi/cc/oxi/life-roulette
pnpm typecheck 2>&1 | head -30
```

Expected: множество ошибок в существующем коде (не передаём новые поля). Это OK на этом шаге — пофиксим в следующих задачах.

- [ ] **Step 7:** Commit

```bash
git add src/game/types.ts
git commit -m "feat: добавить типы для системы классов"
```

### Task A.2: Class factory и дефолтный classState

**Files:**
- Create: `src/game/classes.ts`
- Create: `tests/unit/classes.test.ts`

- [ ] **Step 1:** Failing test

```typescript
// tests/unit/classes.test.ts
import { describe, it, expect } from 'vitest';
import { defaultClassState, initialPlayerForClass, classMaxLives, classStartingInventory } from '../../src/game/classes';
import type { ClassId } from '../../src/game/types';

describe('classes', () => {
  it('defaultClassState returns all-false/zero', () => {
    expect(defaultClassState()).toEqual({
      tankBlockUsed: false,
      armorActive: false,
      armorChargesLeft: 0,
      armorRoundsLeft: 0,
      knifeArmed: false,
      knifeUsed: false,
      lightningUsedThisChamber: false,
      lightningTotalUsed: 0,
    });
  });

  it('initialPlayerForClass(specops) activates armor with 3 charges and 3 rounds', () => {
    const state = initialPlayerForClass('specops');
    expect(state.armorActive).toBe(true);
    expect(state.armorChargesLeft).toBe(3);
    expect(state.armorRoundsLeft).toBe(3);
  });

  it('initialPlayerForClass(null) returns default state', () => {
    expect(initialPlayerForClass(null)).toEqual(defaultClassState());
  });

  it('classMaxLives: double=5, others=4', () => {
    expect(classMaxLives('double')).toBe(5);
    expect(classMaxLives('medic')).toBe(4);
    expect(classMaxLives('tank')).toBe(4);
    expect(classMaxLives('specops')).toBe(4);
    expect(classMaxLives('god')).toBe(4);
    expect(classMaxLives(null)).toBe(4);
  });

  it('classStartingInventory: double has 1 knife, others have 0', () => {
    expect(classStartingInventory('double')).toEqual({ chocolate: 1, magnifier: 1, knife: 1 });
    expect(classStartingInventory('medic')).toEqual({ chocolate: 1, magnifier: 1, knife: 0 });
    expect(classStartingInventory(null)).toEqual({ chocolate: 1, magnifier: 1, knife: 0 });
  });

  it('all 5 classes are defined', () => {
    const classes: (ClassId | null)[] = ['medic', 'tank', 'specops', 'double', 'god', null];
    for (const c of classes) {
      expect(initialPlayerForClass(c)).toBeDefined();
      expect(classMaxLives(c)).toBeGreaterThan(0);
      expect(classStartingInventory(c)).toBeDefined();
    }
  });
});
```

- [ ] **Step 2:** Run — FAIL (no module)

```bash
pnpm test tests/unit/classes.test.ts
```

- [ ] **Step 3:** Implement `src/game/classes.ts`

```typescript
import type { ClassId, ClassState, ItemId } from './types';

export function defaultClassState(): ClassState {
  return {
    tankBlockUsed: false,
    armorActive: false,
    armorChargesLeft: 0,
    armorRoundsLeft: 0,
    knifeArmed: false,
    knifeUsed: false,
    lightningUsedThisChamber: false,
    lightningTotalUsed: 0,
  };
}

export function initialPlayerForClass(classId: ClassId | null): ClassState {
  const base = defaultClassState();
  if (classId === 'specops') {
    return { ...base, armorActive: true, armorChargesLeft: 3, armorRoundsLeft: 3 };
  }
  return base;
}

export function classMaxLives(classId: ClassId | null): number {
  if (classId === 'double') return 5;
  return 4;
}

export function classStartingLives(classId: ClassId | null): number {
  if (classId === 'double') return 5;
  return 4;
}

export function classStartingInventory(classId: ClassId | null): Record<ItemId, number> {
  const base: Record<ItemId, number> = { chocolate: 1, magnifier: 1, knife: 0 };
  if (classId === 'double') {
    return { ...base, knife: 1 };
  }
  return base;
}

export const CLASS_PRICES: Record<ClassId, number> = {
  medic: 200,
  tank: 300,
  specops: 400,
  double: 5000,
  god: 10000,
};

export const ALL_CLASS_IDS: ClassId[] = ['medic', 'tank', 'specops', 'double', 'god'];
```

- [ ] **Step 4:** Run — PASS

```bash
pnpm test tests/unit/classes.test.ts
```

- [ ] **Step 5:** Commit

```bash
git add src/game/classes.ts tests/unit/classes.test.ts
git commit -m "feat: добавить class factory и дефолтные состояния"
```

### Task A.3: Profile.ownedClasses + миграция

**Files:**
- Modify: `src/persistence/profiles.ts`
- Modify: `tests/unit/persistence/profiles.test.ts`

- [ ] **Step 1:** Расширить тесты

```typescript
// добавить в tests/unit/persistence/profiles.test.ts описание блока:
import type { ClassId } from '../../../src/game/types';

describe('profiles ownedClasses', () => {
  it('newly created profile has empty ownedClasses', () => {
    const p = createProfile('Alice');
    expect(p.ownedClasses).toEqual([]);
  });

  it('updateProfile sets ownedClasses', () => {
    const p = createProfile('Alice');
    updateProfile(p.id, { ownedClasses: ['medic', 'tank'] as ClassId[] });
    expect(getProfile(p.id)?.ownedClasses).toEqual(['medic', 'tank']);
  });

  it('migrates legacy profiles (no ownedClasses field) to []', () => {
    memoryStorage.setItem('life-roulette:profiles', JSON.stringify([
      { id: 'legacy', name: 'L', currency: 100, inventory: { chocolate: 0, magnifier: 0 }, createdAt: 1, lastUsed: 1 },
    ]));
    const list = listProfiles();
    expect(list).toHaveLength(1);
    expect(list[0]!.ownedClasses).toEqual([]);
  });
});
```

- [ ] **Step 2:** Run — FAIL

```bash
pnpm test tests/unit/persistence/profiles.test.ts
```

- [ ] **Step 3:** Update `src/persistence/profiles.ts`. Найти interface `PlayerProfile`, добавить `ownedClasses`:

```typescript
import type { ItemId, ClassId } from '../game/types';
import { STORAGE_KEYS } from './schema';

export interface PlayerProfile {
  id: string;
  name: string;
  currency: number;
  inventory: Record<ItemId, number>;
  ownedClasses: ClassId[];     // НОВОЕ
  createdAt: number;
  lastUsed: number;
}
```

Обновить `readAll()` для миграции:

```typescript
function readAll(): PlayerProfile[] {
  try {
    const raw = localStorage.getItem(STORAGE_KEYS.profiles);
    if (!raw) return [];
    const parsed = JSON.parse(raw);
    if (!Array.isArray(parsed)) return [];
    // Migration: legacy profiles without ownedClasses
    return parsed.map((p: PlayerProfile & { ownedClasses?: ClassId[] }) => ({
      ...p,
      ownedClasses: p.ownedClasses ?? [],
    }));
  } catch {
    return [];
  }
}
```

Обновить `createProfile()`:

```typescript
export function createProfile(name: string): PlayerProfile {
  const now = Date.now();
  const p: PlayerProfile = {
    id: generateId(),
    name,
    currency: 0,
    inventory: { chocolate: 0, magnifier: 0, knife: 0 },  // ОБНОВЛЕНО: knife добавлен с 0
    ownedClasses: [],                                      // НОВОЕ
    createdAt: now,
    lastUsed: now,
  };
  const all = readAll();
  all.push(p);
  writeAll(all);
  return p;
}
```

- [ ] **Step 4:** Run — PASS

```bash
pnpm test tests/unit/persistence/profiles.test.ts
```

- [ ] **Step 5:** Commit

```bash
git add src/persistence/profiles.ts tests/unit/persistence/profiles.test.ts
git commit -m "feat: добавить ownedClasses в профиль с миграцией"
```

---

## Phase B — Engine: damage pipeline

### Task B.1: resolveDamage helper (Tank + Specops armor)

**Files:**
- Create: `src/game/damage.ts`
- Create: `tests/unit/damage.test.ts`

- [ ] **Step 1:** Failing tests

```typescript
// tests/unit/damage.test.ts
import { describe, it, expect } from 'vitest';
import { resolveDamage } from '../../src/game/damage';
import type { Player } from '../../src/game/types';
import { defaultClassState, initialPlayerForClass } from '../../src/game/classes';

function makePlayer(overrides: Partial<Player> = {}): Player {
  return {
    id: 'p1', name: 'P1', profileId: null,
    lives: 4,
    inventory: { chocolate: 1, magnifier: 1, knife: 0 },
    isBot: false, eliminated: false,
    classId: null,
    classState: defaultClassState(),
    ...overrides,
  };
}

describe('resolveDamage', () => {
  it('classless player: damage passes through unchanged', () => {
    const target = makePlayer();
    const r = resolveDamage(target, 1, 'bullet');
    expect(r.finalDamage).toBe(1);
    expect(r.updatedClassState).toEqual(target.classState);
    expect(r.events).toEqual([]);
  });

  it('blank/zero damage stays zero (no class trigger)', () => {
    const target = makePlayer({ classId: 'tank' });
    const r = resolveDamage(target, 0, 'bullet');
    expect(r.finalDamage).toBe(0);
    expect(r.updatedClassState.tankBlockUsed).toBe(false);
    expect(r.events).toEqual([]);
  });

  it('tank blocks first incoming bullet (live), sets tankBlockUsed', () => {
    const target = makePlayer({ classId: 'tank' });
    const r = resolveDamage(target, 1, 'bullet');
    expect(r.finalDamage).toBe(0);
    expect(r.updatedClassState.tankBlockUsed).toBe(true);
    expect(r.events).toEqual([{ type: 'tank-block-triggered', playerId: 'p1' }]);
  });

  it('tank does NOT block second bullet (already used)', () => {
    const target = makePlayer({ classId: 'tank', classState: { ...defaultClassState(), tankBlockUsed: true } });
    const r = resolveDamage(target, 1, 'bullet');
    expect(r.finalDamage).toBe(1);
    expect(r.updatedClassState.tankBlockUsed).toBe(true);
    expect(r.events).toEqual([]);
  });

  it('tank does NOT block lightning (only bullets)', () => {
    const target = makePlayer({ classId: 'tank' });
    const r = resolveDamage(target, 1, 'lightning');
    expect(r.finalDamage).toBe(1);
    expect(r.updatedClassState.tankBlockUsed).toBe(false);
  });

  it('specops armor halves bullet damage and consumes 1 charge', () => {
    const target = makePlayer({ classId: 'specops', classState: initialPlayerForClass('specops') });
    const r = resolveDamage(target, 1, 'bullet');
    expect(r.finalDamage).toBe(0.5);
    expect(r.updatedClassState.armorChargesLeft).toBe(2);
    expect(r.updatedClassState.armorActive).toBe(true);
  });

  it('specops armor halves lightning damage too', () => {
    const target = makePlayer({ classId: 'specops', classState: initialPlayerForClass('specops') });
    const r = resolveDamage(target, 1, 'lightning');
    expect(r.finalDamage).toBe(0.5);
    expect(r.updatedClassState.armorChargesLeft).toBe(2);
  });

  it('specops armor breaks at 0 charges, emits armor-broke', () => {
    const target = makePlayer({ classId: 'specops', classState: { ...initialPlayerForClass('specops'), armorChargesLeft: 1 } });
    const r = resolveDamage(target, 1, 'bullet');
    expect(r.finalDamage).toBe(0.5);
    expect(r.updatedClassState.armorActive).toBe(false);
    expect(r.updatedClassState.armorChargesLeft).toBe(0);
    expect(r.events).toEqual([{ type: 'armor-broke', playerId: 'p1' }]);
  });

  it('specops without armor (broken) takes full damage', () => {
    const target = makePlayer({ classId: 'specops', classState: { ...initialPlayerForClass('specops'), armorActive: false } });
    const r = resolveDamage(target, 1, 'bullet');
    expect(r.finalDamage).toBe(1);
  });
});
```

- [ ] **Step 2:** Run — FAIL

- [ ] **Step 3:** Implement `src/game/damage.ts`

```typescript
import type { ClassState, GameEvent, Player } from './types';

export type DamageSource = 'bullet' | 'lightning';

export interface ResolveDamageResult {
  finalDamage: number;
  updatedClassState: ClassState;
  events: GameEvent[];
}

export function resolveDamage(
  target: Player,
  baseDamage: number,
  source: DamageSource,
): ResolveDamageResult {
  const events: GameEvent[] = [];
  let damage = baseDamage;
  let classState: ClassState = { ...target.classState };

  if (damage <= 0) {
    return { finalDamage: 0, updatedClassState: classState, events };
  }

  // Tank: blocks first incoming bullet (not lightning)
  if (target.classId === 'tank' && source === 'bullet' && !classState.tankBlockUsed) {
    classState = { ...classState, tankBlockUsed: true };
    events.push({ type: 'tank-block-triggered', playerId: target.id });
    return { finalDamage: 0, updatedClassState: classState, events };
  }

  // Specops armor: halves any incoming damage, breaks at 0 charges
  if (target.classId === 'specops' && classState.armorActive) {
    damage = damage / 2;
    classState = { ...classState, armorChargesLeft: classState.armorChargesLeft - 1 };
    if (classState.armorChargesLeft <= 0) {
      classState = { ...classState, armorActive: false };
      events.push({ type: 'armor-broke', playerId: target.id });
    }
  }

  return { finalDamage: damage, updatedClassState: classState, events };
}
```

- [ ] **Step 4:** Run — PASS

```bash
pnpm test tests/unit/damage.test.ts
```

- [ ] **Step 5:** Commit

```bash
git add src/game/damage.ts tests/unit/damage.test.ts
git commit -m "feat: добавить resolveDamage с поддержкой Tank и Спецназ брони"
```

### Task B.2: Engine — обновить shoot для использования resolveDamage + Двойник нож

**Files:**
- Modify: `src/game/engine.ts`
- Modify: `tests/unit/engine.test.ts`

- [ ] **Step 1:** Tests for new shoot behavior

```typescript
// добавить в tests/unit/engine.test.ts описание блока:
import { defaultClassState, initialPlayerForClass } from '../../src/game/classes';

describe('engine.applyAction(shoot) with classes', () => {
  function setup3pWithClasses(classes: (ClassId | null)[]) {
    const players = classes.map((c, i) => ({
      ...makePlayer(`p${i}`, `P${i}`),
      classId: c,
      classState: initialPlayerForClass(c),
      lives: c === 'double' ? 5 : 4,
    }));
    let s = initGame(players, 42);
    s = applyAction(s, { type: 'spin-roulette' }).state;
    s = applyAction(s, { type: 'load-chamber' }).state;
    return s;
  }

  it('Tank blocks first live bullet at him (lives unchanged)', () => {
    let state = setup3pWithClasses(['tank', null, null]);
    // Force shooter to be index 1 (P1), shooting tank P0
    state.currentPlayerIndex = 1;
    state = withChamber(state, ['live']);
    const r = applyAction(state, { type: 'shoot', targetId: 'p0' });
    expect(r.state.players[0]!.lives).toBe(4);
    expect(r.state.players[0]!.classState.tankBlockUsed).toBe(true);
    expect(r.events.some(e => e.type === 'tank-block-triggered')).toBe(true);
  });

  it('Specops takes 0.5 damage from live bullet (4 → 3.5)', () => {
    let state = setup3pWithClasses(['specops', null, null]);
    state.currentPlayerIndex = 1;
    state = withChamber(state, ['live']);
    const r = applyAction(state, { type: 'shoot', targetId: 'p0' });
    expect(r.state.players[0]!.lives).toBe(3.5);
    expect(r.state.players[0]!.classState.armorChargesLeft).toBe(2);
  });

  it('Double with knife armed deals 2 damage on live (target 4 → 2)', () => {
    let state = setup3pWithClasses(['double', null, null]);
    state.currentPlayerIndex = 0;
    state.players[0]!.classState.knifeArmed = true;
    state = withChamber(state, ['live']);
    const r = applyAction(state, { type: 'shoot', targetId: 'p1' });
    expect(r.state.players[1]!.lives).toBe(2);
    expect(r.state.players[0]!.classState.knifeArmed).toBe(false);
    expect(r.state.players[0]!.classState.knifeUsed).toBe(true);
    expect(r.events.some(e => e.type === 'knife-doubled-damage')).toBe(true);
  });

  it('Double knife survives blank (still armed after blank shot)', () => {
    let state = setup3pWithClasses(['double', null, null]);
    state.currentPlayerIndex = 0;
    state.players[0]!.classState.knifeArmed = true;
    state = withChamber(state, ['blank', 'live']);
    const r = applyAction(state, { type: 'shoot', targetId: 'p1' });
    expect(r.state.players[0]!.classState.knifeArmed).toBe(true);
    expect(r.state.players[0]!.classState.knifeUsed).toBe(false);
  });

  it('Knife + Specops armor: 2x damage halved = 1 damage', () => {
    let state = setup3pWithClasses(['double', 'specops', null]);
    state.currentPlayerIndex = 0;
    state.players[0]!.classState.knifeArmed = true;
    state = withChamber(state, ['live']);
    const r = applyAction(state, { type: 'shoot', targetId: 'p1' });
    expect(r.state.players[1]!.lives).toBe(3);  // 4 - (2 * 0.5)
  });
});
```

- [ ] **Step 2:** Run — FAIL

- [ ] **Step 3:** Update `src/game/engine.ts` shoot case. Найти `case 'shoot':` и заменить блок вычисления damage на:

```typescript
import { resolveDamage } from './damage';

// в switch case 'shoot':

let baseDamage = fired.bullet === 'live' ? 1 : 0;

// Двойник: knife multiplier on live shots
const shooterIdx = state.currentPlayerIndex;
let players = [...state.players];
if (fired.bullet === 'live' && shooter.classId === 'double' && shooter.classState.knifeArmed) {
  baseDamage *= 2;
  players[shooterIdx] = {
    ...shooter,
    classState: { ...shooter.classState, knifeArmed: false, knifeUsed: true },
  };
  events.push({ type: 'knife-doubled-damage', playerId: shooter.id });
}

// Resolve damage with target's class effects
const targetIdx = players.findIndex(p => p.id === target.id);
const targetCurrent = players[targetIdx]!;
const damageResult = resolveDamage(targetCurrent, baseDamage, 'bullet');
events.push(...damageResult.events);

const damage = damageResult.finalDamage;
const newLives = targetCurrent.lives - damage;
const eliminated = newLives <= 0;

players[targetIdx] = {
  ...targetCurrent,
  lives: newLives,
  eliminated,
  classState: damageResult.updatedClassState,
};

if (damage > 0) {
  events.push({ type: 'lives-changed', playerId: target.id, newLives });
}
if (eliminated && !targetCurrent.eliminated) {
  events.push({ type: 'player-eliminated', playerId: target.id });
}
```

(Существующая логика выпадения / extra-turn / chamber-empty — без изменений)

- [ ] **Step 4:** Run — PASS

```bash
pnpm test tests/unit/engine.test.ts
```

- [ ] **Step 5:** Commit

```bash
git add src/game/engine.ts tests/unit/engine.test.ts
git commit -m "feat: интегрировать resolveDamage и Двойник нож в shoot"
```

### Task B.3: Engine — Медик +3 шоколадка с classMaxLives кап

**Files:**
- Modify: `src/game/engine.ts`
- Modify: `tests/unit/engine.test.ts`

- [ ] **Step 1:** Tests

```typescript
describe('engine.applyAction(use-item chocolate) with class', () => {
  it('Medic chocolate heals +3 (lives 1 → 4)', () => {
    let state = setup3pWithClasses(['medic', null, null]);
    state.currentPlayerIndex = 0;
    state.players[0] = { ...state.players[0]!, lives: 1, inventory: { chocolate: 1, magnifier: 1, knife: 0 } };
    const r = applyAction(state, { type: 'use-item', itemId: 'chocolate' });
    expect(r.state.players[0]!.lives).toBe(4);
  });

  it('Medic chocolate caps at 4 (lives 3 + 3 = capped 4)', () => {
    let state = setup3pWithClasses(['medic', null, null]);
    state.currentPlayerIndex = 0;
    state.players[0] = { ...state.players[0]!, lives: 3, inventory: { chocolate: 1, magnifier: 1, knife: 0 } };
    const r = applyAction(state, { type: 'use-item', itemId: 'chocolate' });
    expect(r.state.players[0]!.lives).toBe(4);
  });

  it('Double has cap 5 (lives 5 → chocolate at 4 → 5)', () => {
    let state = setup3pWithClasses(['double', null, null]);
    state.currentPlayerIndex = 0;
    state.players[0] = { ...state.players[0]!, lives: 4, inventory: { chocolate: 1, magnifier: 1, knife: 1 } };
    const r = applyAction(state, { type: 'use-item', itemId: 'chocolate' });
    expect(r.state.players[0]!.lives).toBe(5);
  });

  it('Classless chocolate heals +1 (lives 2 → 3)', () => {
    let state = setup3pWithClasses([null, null, null]);
    state.currentPlayerIndex = 0;
    state.players[0] = { ...state.players[0]!, lives: 2 };
    const r = applyAction(state, { type: 'use-item', itemId: 'chocolate' });
    expect(r.state.players[0]!.lives).toBe(3);
  });
});
```

- [ ] **Step 2:** Run — FAIL

- [ ] **Step 3:** Update use-item case в engine.ts. Найти текущий обработчик `chocolate` и заменить:

```typescript
import { classMaxLives } from './classes';

// в use-item case:
let newLives = player.lives;
if (action.itemId === 'chocolate') {
  const heal = player.classId === 'medic' ? 3 : 1;
  const maxLives = classMaxLives(player.classId);
  newLives = Math.min(player.lives + heal, maxLives);
  if (newLives !== player.lives) {
    events.push({ type: 'lives-changed', playerId: player.id, newLives });
  }
}
// 'magnifier' эффект — приватный, остаётся как было
// 'knife' — обработка в Task B.4 ниже
```

- [ ] **Step 4:** Run — PASS

- [ ] **Step 5:** Commit

```bash
git add src/game/engine.ts tests/unit/engine.test.ts
git commit -m "feat: Медик аптечка +3 жизни и кап для Двойника 5"
```

### Task B.4: Engine — Двойник use-item knife (взвод)

**Files:**
- Modify: `src/game/engine.ts`
- Modify: `tests/unit/engine.test.ts`

- [ ] **Step 1:** Tests

```typescript
describe('engine.applyAction(use-item knife)', () => {
  it('Double can arm knife once: knifeArmed=true, inventory.knife=0', () => {
    let state = setup3pWithClasses(['double', null, null]);
    state.currentPlayerIndex = 0;
    const r = applyAction(state, { type: 'use-item', itemId: 'knife' });
    expect(r.state.players[0]!.classState.knifeArmed).toBe(true);
    expect(r.state.players[0]!.inventory.knife).toBe(0);
    expect(r.events.some(e => e.type === 'knife-armed')).toBe(true);
  });

  it('Non-Double cannot use knife (no knife in inventory)', () => {
    let state = setup3pWithClasses([null, null, null]);
    state.currentPlayerIndex = 0;
    expect(() => applyAction(state, { type: 'use-item', itemId: 'knife' })).toThrow();
  });

  it('Cannot arm knife twice (already armed → throws)', () => {
    let state = setup3pWithClasses(['double', null, null]);
    state.currentPlayerIndex = 0;
    state.players[0]!.classState.knifeArmed = true;
    state.players[0]!.inventory.knife = 0;
    expect(() => applyAction(state, { type: 'use-item', itemId: 'knife' })).toThrow();
  });
});
```

- [ ] **Step 2:** Run — FAIL

- [ ] **Step 3:** Update use-item case в engine.ts. Добавить обработку `knife`:

```typescript
// в use-item case, после chocolate handling:
let newClassState = player.classState;
if (action.itemId === 'knife') {
  if (player.classId !== 'double') throw new Error('Only Double can use knife');
  if (player.classState.knifeArmed) throw new Error('Knife already armed');
  if (player.classState.knifeUsed) throw new Error('Knife already used this game');
  newClassState = { ...newClassState, knifeArmed: true };
  events.push({ type: 'knife-armed', playerId: player.id });
}

const newPlayers = state.players.map((p, idx) =>
  idx === state.currentPlayerIndex
    ? { ...p, inventory: newInventory, lives: newLives, classState: newClassState }
    : p
);
```

- [ ] **Step 4:** Run — PASS

- [ ] **Step 5:** Commit

```bash
git add src/game/engine.ts tests/unit/engine.test.ts
git commit -m "feat: Двойник может взводить нож (use-item knife)"
```

### Task B.5: Engine — use-ability lightning (Бог)

**Files:**
- Modify: `src/game/engine.ts`
- Modify: `tests/unit/engine.test.ts`

- [ ] **Step 1:** Tests

```typescript
describe('engine.applyAction(use-ability lightning)', () => {
  it('God damages target with lightning (1 dmg)', () => {
    let state = setup3pWithClasses(['god', null, null]);
    state.currentPlayerIndex = 0;
    state.phase = 'turn-item';
    const r = applyAction(state, { type: 'use-ability', ability: 'lightning', targetId: 'p1' });
    expect(r.state.players[1]!.lives).toBe(3);
    expect(r.state.players[0]!.classState.lightningTotalUsed).toBe(1);
    expect(r.state.players[0]!.classState.lightningUsedThisChamber).toBe(true);
    expect(r.events.some(e => e.type === 'lightning-cast')).toBe(true);
  });

  it('Non-God throws on lightning', () => {
    let state = setup3pWithClasses([null, null, null]);
    state.currentPlayerIndex = 0;
    expect(() => applyAction(state, { type: 'use-ability', ability: 'lightning', targetId: 'p1' })).toThrow();
  });

  it('Cannot lightning self', () => {
    let state = setup3pWithClasses(['god', null, null]);
    state.currentPlayerIndex = 0;
    expect(() => applyAction(state, { type: 'use-ability', ability: 'lightning', targetId: 'p0' })).toThrow();
  });

  it('Lightning blocked by Specops armor (1 → 0.5)', () => {
    let state = setup3pWithClasses(['god', 'specops', null]);
    state.currentPlayerIndex = 0;
    const r = applyAction(state, { type: 'use-ability', ability: 'lightning', targetId: 'p1' });
    expect(r.state.players[1]!.lives).toBe(3.5);
  });

  it('Lightning NOT blocked by Tank (Tank blocks bullets only)', () => {
    let state = setup3pWithClasses(['god', 'tank', null]);
    state.currentPlayerIndex = 0;
    const r = applyAction(state, { type: 'use-ability', ability: 'lightning', targetId: 'p1' });
    expect(r.state.players[1]!.lives).toBe(3);
    expect(r.state.players[1]!.classState.tankBlockUsed).toBe(false);
  });

  it('Cannot use lightning twice in same chamber', () => {
    let state = setup3pWithClasses(['god', null, null]);
    state.currentPlayerIndex = 0;
    state.players[0]!.classState.lightningUsedThisChamber = true;
    expect(() => applyAction(state, { type: 'use-ability', ability: 'lightning', targetId: 'p1' })).toThrow();
  });

  it('Cannot use lightning if total limit reached (4)', () => {
    let state = setup3pWithClasses(['god', null, null]);
    state.currentPlayerIndex = 0;
    state.players[0]!.classState.lightningTotalUsed = 4;
    expect(() => applyAction(state, { type: 'use-ability', ability: 'lightning', targetId: 'p1' })).toThrow();
  });

  it('Lightning does not change phase (shoot still required)', () => {
    let state = setup3pWithClasses(['god', null, null]);
    state.currentPlayerIndex = 0;
    state.phase = 'turn-item';
    const r = applyAction(state, { type: 'use-ability', ability: 'lightning', targetId: 'p1' });
    expect(r.state.phase).toBe('turn-item');
  });
});
```

- [ ] **Step 2:** Run — FAIL

- [ ] **Step 3:** Add new case to engine.ts switch:

```typescript
    case 'use-ability': {
      if (action.ability !== 'lightning') {
        throw new Error(`Unknown ability: ${(action as { ability: string }).ability}`);
      }
      const caster = state.players[state.currentPlayerIndex]!;
      if (caster.classId !== 'god') throw new Error('Only god can cast lightning');
      if (caster.classState.lightningUsedThisChamber) throw new Error('Lightning already used this chamber');
      if (caster.classState.lightningTotalUsed >= 4) throw new Error('Lightning total limit reached');

      const targetIdx = state.players.findIndex(p => p.id === action.targetId);
      if (targetIdx < 0) throw new Error(`Target ${action.targetId} not found`);
      const target = state.players[targetIdx]!;
      if (target.eliminated) throw new Error('Target eliminated');
      if (target.id === caster.id) throw new Error('Cannot cast lightning on self');

      const damageResult = resolveDamage(target, 1, 'lightning');
      events.push(...damageResult.events);

      const finalDamage = damageResult.finalDamage;
      const newLives = target.lives - finalDamage;
      const eliminated = newLives <= 0;

      const newPlayers = [...state.players];
      newPlayers[targetIdx] = {
        ...target,
        lives: newLives,
        eliminated,
        classState: damageResult.updatedClassState,
      };
      newPlayers[state.currentPlayerIndex] = {
        ...caster,
        classState: {
          ...caster.classState,
          lightningUsedThisChamber: true,
          lightningTotalUsed: caster.classState.lightningTotalUsed + 1,
        },
      };

      events.push({ type: 'lightning-cast', casterId: caster.id, targetId: target.id, damage: finalDamage });
      if (finalDamage > 0) {
        events.push({ type: 'lives-changed', playerId: target.id, newLives });
      }
      if (eliminated && !target.eliminated) {
        events.push({ type: 'player-eliminated', playerId: target.id });
      }

      return {
        state: { ...state, players: newPlayers, actionLog: [...state.actionLog, action] },
        events,
      };
    }
```

- [ ] **Step 4:** Run — PASS

- [ ] **Step 5:** Commit

```bash
git add src/game/engine.ts tests/unit/engine.test.ts
git commit -m "feat: Бог может стрелять молнией (use-ability lightning)"
```

### Task B.6: Engine — load-chamber resets per-chamber state

**Files:**
- Modify: `src/game/engine.ts`
- Modify: `tests/unit/engine.test.ts`

- [ ] **Step 1:** Tests

```typescript
describe('engine.applyAction(load-chamber) resets per-chamber class state', () => {
  it('God lightning per-chamber resets to false on new chamber', () => {
    let state = setup3pWithClasses(['god', null, null]);
    state.currentPlayerIndex = 0;
    state.players[0]!.classState.lightningUsedThisChamber = true;
    state.phase = 'between-rounds';
    state = applyAction(state, { type: 'spin-roulette' }).state;
    const r = applyAction(state, { type: 'load-chamber' });
    const god = r.state.players.find(p => p.classId === 'god')!;
    expect(god.classState.lightningUsedThisChamber).toBe(false);
  });

  it('Specops armor decrements armorRoundsLeft on load-chamber', () => {
    let state = setup3pWithClasses(['specops', null, null]);
    state.phase = 'between-rounds';
    state = applyAction(state, { type: 'spin-roulette' }).state;
    const r = applyAction(state, { type: 'load-chamber' });
    const specops = r.state.players.find(p => p.classId === 'specops')!;
    expect(specops.classState.armorRoundsLeft).toBe(2);
    expect(specops.classState.armorActive).toBe(true);
  });

  it('Specops armor breaks when armorRoundsLeft reaches 0', () => {
    let state = setup3pWithClasses(['specops', null, null]);
    state.players[0]!.classState.armorRoundsLeft = 1;
    state.phase = 'between-rounds';
    state = applyAction(state, { type: 'spin-roulette' }).state;
    const r = applyAction(state, { type: 'load-chamber' });
    const specops = r.state.players.find(p => p.classId === 'specops')!;
    expect(specops.classState.armorActive).toBe(false);
    expect(r.events.some(e => e.type === 'armor-broke' && (e as { playerId: string }).playerId === 'p0')).toBe(true);
  });
});
```

- [ ] **Step 2:** Run — FAIL

- [ ] **Step 3:** Update load-chamber case в engine.ts. После генерации chamber, добавить логику резета per-chamber state и armor rounds:

```typescript
    case 'load-chamber': {
      const chamber = generateChamber(rng);
      events.push({ type: 'chamber-loaded', liveCount: chamber.liveCount, blankCount: chamber.blankCount });

      const newPlayers = state.players.map(p => {
        let cs = { ...p.classState, lightningUsedThisChamber: false };

        if (p.classId === 'specops' && cs.armorActive) {
          cs.armorRoundsLeft -= 1;
          if (cs.armorRoundsLeft <= 0) {
            cs.armorActive = false;
            events.push({ type: 'armor-broke', playerId: p.id });
          }
        }

        return { ...p, classState: cs };
      });

      return {
        state: {
          ...state,
          chamber,
          players: newPlayers,
          phase: 'turn-item',
          rngState: rng.toState(),
          extraTurnsUsedThisChamber: {},
          itemsUsedThisTurn: { chocolate: false, magnifier: false, knife: false },
          actionLog: [...state.actionLog, action],
        },
        events,
      };
    }
```

(Note: itemsUsedThisTurn нужно расширить с `knife: false` тоже)

Также: проверить тип `Record<ItemId, boolean>` в types.ts — он автоматически расширится с `knife`. Но инициализация во многих местах нужна обновить. Найти все `{ chocolate: false, magnifier: false }` и добавить `knife: false`.

```bash
grep -rn "chocolate: false, magnifier: false" src/ tests/
```

Заменить везде на `{ chocolate: false, magnifier: false, knife: false }`.

- [ ] **Step 4:** Run — PASS

- [ ] **Step 5:** Commit

```bash
git add src/game/engine.ts src/game/types.ts tests/
git commit -m "feat: load-chamber сбрасывает per-chamber состояние классов"
```

### Task B.7: Engine — initGame использует class factory

**Files:**
- Modify: `src/game/engine.ts`

- [ ] **Step 1:** initGame должен корректно инициализировать классы. Найти `initGame()` и обновить:

```typescript
import { initialPlayerForClass, classStartingLives, classStartingInventory } from './classes';

export function initGame(players: Player[], seed: number): GameState {
  if (players.length < 2 || players.length > 7) {
    throw new Error(`Invalid player count: ${players.length}`);
  }
  return {
    players: players.map(p => ({
      ...p,
      lives: classStartingLives(p.classId),
      inventory: classStartingInventory(p.classId),
      classState: initialPlayerForClass(p.classId),
      eliminated: false,
    })),
    currentPlayerIndex: 0,
    chamber: { bullets: [], liveCount: 0, blankCount: 0 },
    phase: 'roulette',
    rngSeed: seed,
    rngState: seed >>> 0,
    extraTurnsUsedThisChamber: {},
    itemsUsedThisTurn: { chocolate: false, magnifier: false, knife: false },
    winnerId: null,
    actionLog: [],
  };
}
```

- [ ] **Step 2:** Run all engine tests

```bash
pnpm test tests/unit/engine.test.ts
```

Expected: все pass. Если есть failures из-за изменений в initGame — обновить тесты которые ассертят начальное состояние.

- [ ] **Step 3:** Commit

```bash
git add src/game/engine.ts
git commit -m "feat: initGame использует class factory для жизней/инвентаря/состояния"
```

---

## Phase C — Bot updates

### Task C.1: Bot выбирает молнию когда есть слабый враг (Бог)

**Files:**
- Modify: `src/game/bot.ts`
- Modify: `tests/unit/bot.test.ts`

- [ ] **Step 1:** Tests

```typescript
describe('botDecide for God class', () => {
  it('God with lightning available + weak enemy → cast lightning', () => {
    const view: PlayerView = {
      selfPlayer: {
        id: 'bot', name: 'Bot', profileId: null, isBot: true, eliminated: false,
        lives: 4,
        inventory: { chocolate: 1, magnifier: 1, knife: 0 },
        classId: 'god',
        classState: { ...defaultClassState() },
      },
      otherPlayers: [{ id: 'h', name: 'H', lives: 1, eliminated: false, classId: null }],
      chamberLive: 3, chamberBlank: 3, currentPlayerId: 'bot', phase: 'turn-item',
    };
    const a = botDecide(view, createRng(1), { magnifierUsedThisTurn: false, peekedNextBullet: null });
    expect(a).toEqual({ type: 'use-ability', ability: 'lightning', targetId: 'h' });
  });

  it('God without lightning charges → falls through to magnifier', () => {
    const view: PlayerView = {
      selfPlayer: {
        id: 'bot', name: 'Bot', profileId: null, isBot: true, eliminated: false,
        lives: 4,
        inventory: { chocolate: 1, magnifier: 1, knife: 0 },
        classId: 'god',
        classState: { ...defaultClassState(), lightningTotalUsed: 4 },
      },
      otherPlayers: [{ id: 'h', name: 'H', lives: 1, eliminated: false, classId: null }],
      chamberLive: 3, chamberBlank: 3, currentPlayerId: 'bot', phase: 'turn-item',
    };
    const a = botDecide(view, createRng(1), { magnifierUsedThisTurn: false, peekedNextBullet: null });
    expect(a).toEqual({ type: 'use-item', itemId: 'magnifier' });
  });
});
```

- [ ] **Step 2:** Run — FAIL

- [ ] **Step 3:** Update bot.ts. В начало `botDecide`, перед существующей magnifier-проверкой:

```typescript
// God: lightning a weak enemy first if possible
if (
  me.classId === 'god' &&
  !me.classState.lightningUsedThisChamber &&
  me.classState.lightningTotalUsed < 4
) {
  const killable = aliveEnemies.find(e => e.lives <= 1);
  if (killable) {
    return { type: 'use-ability', ability: 'lightning', targetId: killable.id };
  }
}
```

- [ ] **Step 4:** Run — PASS

- [ ] **Step 5:** Commit

```bash
git add src/game/bot.ts tests/unit/bot.test.ts
git commit -m "feat: Бог-бот стреляет молнией в слабого врага"
```

### Task C.2: Bot взводит нож (Двойник)

**Files:**
- Modify: `src/game/bot.ts`
- Modify: `tests/unit/bot.test.ts`

- [ ] **Step 1:** Tests

```typescript
describe('botDecide for Double class', () => {
  it('Double sees peeked=live + has knife + weak enemy → arm knife', () => {
    const view: PlayerView = {
      selfPlayer: {
        id: 'bot', name: 'Bot', profileId: null, isBot: true, eliminated: false,
        lives: 5,
        inventory: { chocolate: 1, magnifier: 1, knife: 1 },
        classId: 'double',
        classState: { ...defaultClassState() },
      },
      otherPlayers: [{ id: 'h', name: 'H', lives: 2, eliminated: false, classId: null }],
      chamberLive: 3, chamberBlank: 3, currentPlayerId: 'bot', phase: 'turn-item',
    };
    const a = botDecide(view, createRng(1), { magnifierUsedThisTurn: true, peekedNextBullet: 'live' });
    expect(a).toEqual({ type: 'use-item', itemId: 'knife' });
  });

  it('Double without knife in inventory falls through', () => {
    const view: PlayerView = {
      selfPlayer: {
        id: 'bot', name: 'Bot', profileId: null, isBot: true, eliminated: false,
        lives: 5,
        inventory: { chocolate: 1, magnifier: 1, knife: 0 },
        classId: 'double',
        classState: { ...defaultClassState(), knifeUsed: true },
      },
      otherPlayers: [{ id: 'h', name: 'H', lives: 2, eliminated: false, classId: null }],
      chamberLive: 3, chamberBlank: 3, currentPlayerId: 'bot', phase: 'turn-item',
    };
    const a = botDecide(view, createRng(1), { magnifierUsedThisTurn: true, peekedNextBullet: 'live' });
    expect(a).toEqual({ type: 'shoot', targetId: 'h' });
  });
});
```

- [ ] **Step 2:** Run — FAIL

- [ ] **Step 3:** Update bot.ts. После magnifier-проверки, ДО chocolate:

```typescript
// Double: arm knife if peeked live + weak enemy + knife available
if (
  me.classId === 'double' &&
  me.inventory.knife > 0 &&
  !me.classState.knifeArmed &&
  !me.classState.knifeUsed &&
  context.peekedNextBullet === 'live' &&
  aliveEnemies.some(e => e.lives <= 2)
) {
  return { type: 'use-item', itemId: 'knife' };
}
```

- [ ] **Step 4:** Run — PASS

- [ ] **Step 5:** Commit

```bash
git add src/game/bot.ts tests/unit/bot.test.ts
git commit -m "feat: Двойник-бот взводит нож при peek=live и слабом враге"
```

---

## Phase D — Views

### Task D.1: getPlayerView includes classId in otherPlayers

**Files:**
- Modify: `src/game/views.ts`
- Modify: `tests/unit/views.test.ts`

- [ ] **Step 1:** Test

```typescript
// в views.test.ts добавить:
it('otherPlayers expose classId (public info)', () => {
  let s = initGame([
    { ...p('a','A'), classId: 'medic' },
    { ...p('b','B'), classId: null },
  ], 1);
  const v = getPlayerView(s, 'a');
  expect(v.otherPlayers[0]!.classId).toBe(null);
  // нет classState в other (приватное)
  expect((v.otherPlayers[0] as unknown as { classState?: unknown }).classState).toBeUndefined();
});
```

- [ ] **Step 2:** Run — FAIL

- [ ] **Step 3:** Update views.ts:

```typescript
export function getPlayerView(state: GameState, playerId: string): PlayerView {
  const self = state.players.find(p => p.id === playerId);
  if (!self) throw new Error(`Player ${playerId} not in state`);
  return {
    selfPlayer: self,
    otherPlayers: state.players
      .filter(p => p.id !== playerId)
      .map(p => ({ id: p.id, name: p.name, lives: p.lives, eliminated: p.eliminated, classId: p.classId })),
    chamberLive: state.chamber.liveCount,
    chamberBlank: state.chamber.blankCount,
    currentPlayerId: state.players[state.currentPlayerIndex]!.id,
    phase: state.phase,
  };
}
```

- [ ] **Step 4:** Run — PASS

- [ ] **Step 5:** Commit

```bash
git add src/game/views.ts tests/unit/views.test.ts
git commit -m "feat: getPlayerView включает classId в otherPlayers"
```

---

## Phase E — UI

### Task E.1: i18n — class names + descriptions

**Files:**
- Modify: `src/i18n.ts`

- [ ] **Step 1:** Найти существующий `i18n.ts`. Добавить класс-связанные строки:

```typescript
// добавить в существующий i18n.ts объект:
export const CLASS_NAMES: Record<string, string> = {
  medic: 'Медик',
  tank: 'Танк',
  specops: 'Спецназ',
  double: 'Двойник',
  god: 'Бог',
};

export const CLASS_ICONS: Record<string, string> = {
  medic: '🩺',
  tank: '🛡',
  specops: '🎯',
  double: '🤺',
  god: '⚡',
};

export const CLASS_DESCRIPTIONS: Record<string, string> = {
  medic: 'Шоколадка восстанавливает +3 жизни',
  tank: 'Один раз за игру блокирует боевой выстрел',
  specops: 'Стартует в броне (3 раунда / 3 хита, урон ÷2)',
  double: 'Старт с 5 жизнями + нож (1× за игру 2× урон)',
  god: 'Молния: 1 урон, 1 раз/раунд, 4 раз за игру',
};

export const CLASS_NONE = 'Без класса';
```

- [ ] **Step 2:** Commit

```bash
git add src/i18n.ts
git commit -m "feat: i18n строки для классов"
```

### Task E.2: ClassSelectScreen

**Files:**
- Create: `src/menu/ClassSelectScreen.ts`

- [ ] **Step 1:** Implement

```typescript
// src/menu/ClassSelectScreen.ts
import type { ClassId } from '../game/types';
import type { PlayerProfile } from '../persistence/profiles';
import { CLASS_NAMES, CLASS_ICONS, CLASS_DESCRIPTIONS, CLASS_NONE } from '../i18n';

export interface ClassSelectScreenProps {
  playerName: string;
  ownedClasses: ClassId[];
  onSelected: (classId: ClassId | null) => void;
}

export function mountClassSelectScreen(parent: HTMLElement, props: ClassSelectScreenProps): { unmount: () => void } {
  const root = document.createElement('div');
  root.style.cssText = 'position:fixed;inset:0;background:#0a0a14;color:#e8e8f0;display:flex;flex-direction:column;align-items:center;justify-content:center;padding:20px;z-index:500;overflow-y:auto;';

  const title = document.createElement('h2');
  title.textContent = `${props.playerName}, выбери класс`;
  title.style.cssText = 'font-size:24px;margin-bottom:24px;text-align:center;';
  root.appendChild(title);

  const list = document.createElement('div');
  list.style.cssText = 'display:flex;flex-direction:column;gap:12px;width:100%;max-width:400px;';

  const makeCard = (classId: ClassId | null, label: string, icon: string, description: string): HTMLDivElement => {
    const card = document.createElement('div');
    card.style.cssText = 'background:#1a1a2e;border:2px solid #444;border-radius:12px;padding:16px;cursor:pointer;display:flex;align-items:center;gap:16px;min-height:80px;';
    card.onmouseenter = () => { card.style.borderColor = '#6c5ce7'; };
    card.onmouseleave = () => { card.style.borderColor = '#444'; };
    card.onclick = () => { props.onSelected(classId); };

    const iconEl = document.createElement('div');
    iconEl.textContent = icon;
    iconEl.style.cssText = 'font-size:36px;flex-shrink:0;';

    const textWrap = document.createElement('div');
    const nameEl = document.createElement('div');
    nameEl.textContent = label;
    nameEl.style.cssText = 'font-weight:bold;font-size:18px;margin-bottom:4px;';
    const descEl = document.createElement('div');
    descEl.textContent = description;
    descEl.style.cssText = 'font-size:14px;color:#aaa;';
    textWrap.append(nameEl, descEl);

    card.append(iconEl, textWrap);
    return card;
  };

  list.appendChild(makeCard(null, CLASS_NONE, '⚪', 'Стандартные правила'));
  for (const classId of props.ownedClasses) {
    list.appendChild(makeCard(classId, CLASS_NAMES[classId]!, CLASS_ICONS[classId]!, CLASS_DESCRIPTIONS[classId]!));
  }

  root.appendChild(list);
  parent.appendChild(root);

  return { unmount: () => root.remove() };
}
```

- [ ] **Step 2:** Typecheck

```bash
pnpm typecheck
```

- [ ] **Step 3:** Commit

```bash
git add src/menu/ClassSelectScreen.ts
git commit -m "feat: ClassSelectScreen — выбор класса перед игрой"
```

### Task E.3: PlayerListHud — class icon + fractional hearts

**Files:**
- Modify: `src/hud/PlayerListHud.ts`

- [ ] **Step 1:** Найти текущий PlayerListHud. Добавить:
- Иконку класса рядом с именем
- Дробные жизни как полные/половинные сердечки

Пример новой функции для отрисовки сердечек:

```typescript
function renderHearts(lives: number, maxLives: number): HTMLElement {
  const wrap = document.createElement('span');
  wrap.style.cssText = 'display:inline-flex;gap:2px;';
  let remaining = lives;
  for (let i = 0; i < maxLives; i++) {
    const heart = document.createElement('span');
    heart.style.cssText = 'font-size:14px;';
    if (remaining >= 1) {
      heart.textContent = '❤';
      remaining -= 1;
    } else if (remaining === 0.5) {
      heart.textContent = '💔';
      remaining = 0;
    } else {
      heart.textContent = '🤍';
    }
    wrap.appendChild(heart);
  }
  return wrap;
}
```

И в render-функции для каждого игрока добавить иконку класса:

```typescript
import { CLASS_ICONS } from '../i18n';

// при отрисовке игрока:
if (player.classId) {
  const icon = document.createElement('span');
  icon.textContent = CLASS_ICONS[player.classId] ?? '';
  icon.style.cssText = 'margin-right:4px;';
  card.appendChild(icon);
}
```

(Полная имплементация зависит от существующей структуры PlayerListHud — править вокруг существующего кода)

- [ ] **Step 2:** Manual verify

```bash
pnpm dev
```

Открыть `http://localhost:5173/life-roulette/`, начать игру vs AI, выбрать класс — увидеть иконку класса и правильные сердечки в HUD.

- [ ] **Step 3:** Commit

```bash
git add src/hud/PlayerListHud.ts
git commit -m "feat: PlayerListHud показывает иконку класса и дробные жизни"
```

### Task E.4: ItemBarHud — knife + lightning

**Files:**
- Modify: `src/hud/ItemBarHud.ts`

- [ ] **Step 1:** Найти текущий ItemBarHud. Добавить:
- Кнопку ножа (только для Двойника, если `inventory.knife > 0` и `!knifeArmed`)
- Кнопку молнии (только для Бога, если `lightningTotalUsed < 4 && !lightningUsedThisChamber`)
- Показать состояние "нож взведён" иконкой

```typescript
import { CLASS_ICONS } from '../i18n';

// в render для текущего игрока:

// Knife (Double only)
if (player.classId === 'double' && player.inventory.knife > 0 && !player.classState.knifeArmed) {
  const btn = makeItemButton('🔪 Взвести нож', () => props.onKnifeArm());
  bar.appendChild(btn);
}
if (player.classId === 'double' && player.classState.knifeArmed) {
  const indicator = document.createElement('span');
  indicator.textContent = '🔪 ВЗВЕДЁН';
  indicator.style.cssText = 'color:#e74c3c;font-weight:bold;';
  bar.appendChild(indicator);
}

// Lightning (God only)
if (player.classId === 'god') {
  const used = player.classState.lightningTotalUsed;
  const usedThisChamber = player.classState.lightningUsedThisChamber;
  const btn = makeItemButton(`⚡ Молния (${4 - used}/4)`, () => props.onLightningRequest());
  if (used >= 4 || usedThisChamber) {
    btn.disabled = true;
    btn.style.opacity = '0.5';
  }
  bar.appendChild(btn);
}
```

(Точные сигнатуры зависят от существующего ItemBarHud. `props.onKnifeArm` и `props.onLightningRequest` нужно добавить в ItemBarHudProps)

- [ ] **Step 2:** Manual verify

- [ ] **Step 3:** Commit

```bash
git add src/hud/ItemBarHud.ts
git commit -m "feat: ItemBarHud показывает нож (Двойник) и молнию (Бог)"
```

### Task E.5: ShopScreen — секция классов

**Files:**
- Modify: `src/menu/ShopScreen.ts`

- [ ] **Step 1:** Найти текущий ShopScreen. Добавить под существующими товарами секцию "Классы":

```typescript
import { ALL_CLASS_IDS, CLASS_PRICES } from '../game/classes';
import { CLASS_NAMES, CLASS_ICONS, CLASS_DESCRIPTIONS } from '../i18n';

// в render-функции после items section:

const classSection = document.createElement('div');
classSection.style.cssText = 'margin-top:24px;';

const classTitle = document.createElement('h3');
classTitle.textContent = 'Классы (разовая покупка)';
classTitle.style.cssText = 'font-size:18px;margin-bottom:12px;';
classSection.appendChild(classTitle);

for (const classId of ALL_CLASS_IDS) {
  const owned = profile.ownedClasses.includes(classId);
  const price = CLASS_PRICES[classId];
  const canAfford = profile.currency >= price;

  const row = document.createElement('div');
  row.style.cssText = 'display:flex;align-items:center;gap:12px;padding:12px;background:#1a1a2e;border-radius:8px;margin-bottom:8px;';

  const icon = document.createElement('span');
  icon.textContent = CLASS_ICONS[classId];
  icon.style.cssText = 'font-size:24px;';

  const text = document.createElement('div');
  text.style.cssText = 'flex:1;';
  const name = document.createElement('div');
  name.textContent = CLASS_NAMES[classId];
  name.style.cssText = 'font-weight:bold;';
  const desc = document.createElement('div');
  desc.textContent = CLASS_DESCRIPTIONS[classId];
  desc.style.cssText = 'font-size:12px;color:#aaa;';
  text.append(name, desc);

  const btn = document.createElement('button');
  if (owned) {
    btn.textContent = '✓ Куплено';
    btn.disabled = true;
    btn.style.cssText = 'background:#2ecc71;color:white;border:none;padding:8px 16px;border-radius:6px;';
  } else {
    btn.textContent = `${price} ₽`;
    btn.disabled = !canAfford;
    btn.style.cssText = `background:${canAfford ? '#6c5ce7' : '#444'};color:white;border:none;padding:8px 16px;border-radius:6px;cursor:${canAfford ? 'pointer' : 'not-allowed'};`;
    btn.onclick = () => props.onBuyClass(classId);
  }

  row.append(icon, text, btn);
  classSection.appendChild(row);
}

container.appendChild(classSection);
```

И в `ShopScreenProps`:
```typescript
onBuyClass: (classId: ClassId) => void;
```

В обработчике `onBuyClass`: списать валюту, добавить в `profile.ownedClasses`, обновить через `updateProfile`, перерендерить.

- [ ] **Step 2:** Manual verify через `pnpm dev`. Зайти в магазин — увидеть классы.

- [ ] **Step 3:** Commit

```bash
git add src/menu/ShopScreen.ts
git commit -m "feat: ShopScreen — секция покупки классов"
```

---

## Phase F — Controller integration

### Task F.1: OfflineGameController — class select flow

**Files:**
- Modify: `src/controllers/start-game.ts`
- Modify: `src/controllers/OfflineGameController.ts`

- [ ] **Step 1:** В `start-game.ts` (или где собираются игроки и стартуется игра): после ProfileSelect, для КАЖДОГО игрока (vs AI: 1 игрок, hot-seat: N игроков) — показать ClassSelectScreen, дождаться выбора, передать `classId` в `Player`.

```typescript
import { mountClassSelectScreen } from '../menu/ClassSelectScreen';
import type { ClassId } from '../game/types';

async function selectClassForProfile(parent: HTMLElement, profile: PlayerProfile): Promise<ClassId | null> {
  return new Promise(resolve => {
    const screen = mountClassSelectScreen(parent, {
      playerName: profile.name,
      ownedClasses: profile.ownedClasses,
      onSelected: (classId) => {
        screen.unmount();
        resolve(classId);
      },
    });
  });
}

// в startGameVsAI / startGameHotSeat:
// для каждого человеческого игрока — await selectClassForProfile()
// для бота — random выбор:
import { ALL_CLASS_IDS } from '../game/classes';
function randomBotClass(rng: Rng): ClassId | null {
  // 50% chance no class, 50% any class
  if (rng.next() < 0.5) return null;
  return ALL_CLASS_IDS[rng.intRange(0, ALL_CLASS_IDS.length - 1)] ?? null;
}
```

Передать `classId` в конструктор Player при инициализации.

- [ ] **Step 2:** OfflineGameController — обработка action `use-item: knife` и `use-ability: lightning`:

В цикле обработки человеческих действий: добавить новые типы кнопок:
- "Взвести нож" → `applyAction({ type: 'use-item', itemId: 'knife' })`
- "Молния" → выбор цели через UI, потом `applyAction({ type: 'use-ability', ability: 'lightning', targetId })`

Бот: `botDecide` уже возвращает action нужного типа — диспетчер просто применяет.

- [ ] **Step 3:** Manual playtest

```bash
pnpm dev
```

Открыть, начать vs AI, выбрать Бога, проиграть несколько ходов — молния должна работать.

- [ ] **Step 4:** Commit

```bash
git add src/controllers/start-game.ts src/controllers/OfflineGameController.ts
git commit -m "feat: интеграция class select flow и новых action в контроллер"
```

---

## Phase G — e2e + final integration

### Task G.1: Playwright e2e — играем как Медик

**Files:**
- Modify: `tests/e2e/play-vs-ai.spec.cjs` (или новый файл `play-as-medic.spec.cjs`)

- [ ] **Step 1:** Создать e2e тест что играет за Медика:
  1. Открыть игру
  2. Открыть магазин, купить Медика (нужно дать стартовую валюту через `localStorage.setItem` инжект)
  3. Закрыть магазин
  4. Начать игру vs AI
  5. На ClassSelectScreen выбрать Медика
  6. Сыграть до конца
  7. Проверить что hearts обновляются (хотя бы один раз увидеть `❤❤❤❤` или подобное)

```javascript
// tests/e2e/play-as-medic.spec.cjs
const { test, expect } = require('@playwright/test');

test('vs AI as Medic — playthrough', async ({ page }) => {
  // Inject currency via localStorage before page loads
  await page.addInitScript(() => {
    const profiles = [{
      id: 'p1', name: 'Tester', currency: 1000,
      inventory: { chocolate: 0, magnifier: 0, knife: 0 },
      ownedClasses: ['medic'],
      createdAt: Date.now(), lastUsed: Date.now(),
    }];
    localStorage.setItem('life-roulette:profiles', JSON.stringify(profiles));
  });

  await page.goto('/');
  await page.click('text=Играть');
  await page.click('text=vs компьютер');
  await page.click('text=Tester');
  // ClassSelectScreen
  await page.click('text=Медик');
  // Game starts
  await expect(page.locator('text=Стрелять')).toBeVisible({ timeout: 30000 });
  // Hammering shoot until winner
  for (let i = 0; i < 200; i++) {
    if (await page.locator('text=Победил').isVisible().catch(() => false)) break;
    const shoot = page.locator('text=Стрелять').first();
    if (await shoot.isVisible()) await shoot.click();
    const target = page.locator('button:has-text("Bot"), button:has-text("Tester")').first();
    if (await target.isVisible()) await target.click();
    await page.waitForTimeout(300);
  }
  await expect(page.locator('text=Победил')).toBeVisible();
});
```

- [ ] **Step 2:** Run

```bash
pnpm exec playwright test tests/e2e/play-as-medic.spec.cjs
```

Expected: PASS.

- [ ] **Step 3:** Commit

```bash
git add tests/e2e/play-as-medic.spec.cjs
git commit -m "test: e2e — играем за Медика, полный цикл"
```

### Task G.2: All tests + typecheck + build

- [ ] **Step 1:** Run everything

```bash
pnpm typecheck
pnpm test
pnpm build
pnpm exec playwright test
```

Expected: всё зелёное. Если есть failures — фиксить.

- [ ] **Step 2:** Push

```bash
git push
```

- [ ] **Step 3:** Создать PR в GitHub

```bash
gh pr create --title "Phase 1.5a: классы персонажей" --body "$(cat <<'EOF'
## Summary

Добавлены 5 классов персонажей: Медик (200), Танк (300), Спецназ (400), Двойник (5000), Бог (10000).

## Изменения

- Класс — разовая покупка в магазине, открывает использование в любых играх
- ClassSelectScreen перед началом каждой игры (для каждого hot-seat игрока)
- Дробные жизни (шаг 0.5) для брони Спецназа
- Новый ItemId knife (Двойник) и AbilityId lightning (Бог)
- Bot выбирает случайный класс
- HUD показывает иконку класса + сердечки (полные/половинные/пустые)

## Test plan

- [ ] Все юнит-тесты зелёные (60+ старых + новые)
- [ ] Playwright e2e: vs AI happy path + as Medic
- [ ] Manual: каждый класс играется без ошибок (vs AI)
- [ ] Manual: hot-seat 3 игрока с разными классами
- [ ] Manual: дробные жизни корректно отображаются у Спецназа

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

- [ ] **Step 4:** Дождаться зелёного CI и замержить

```bash
gh run watch
gh pr merge --squash --delete-branch
```

- [ ] **Step 5:** Verify live

```bash
sleep 30  # подождать деплой
curl -sI https://oxi-717.github.io/life-roulette/ | head -3
```

Expected: HTTP 200, актуальный last-modified.

---

## Self-Review

После написания плана проверил:

✅ **Spec coverage:**
- §5.1 Медик → Task B.3
- §5.2 Танк → Task B.1 (resolveDamage handles Tank block)
- §5.3 Спецназ → Task B.1 (armor in resolveDamage) + Task B.6 (round-based decay)
- §5.4 Двойник нож → Task B.2 + B.4
- §5.5 Бог молния → Task B.5
- §6 Игрок без класса → covered by null defaults across tasks
- §7 Магазин → Task E.5
- §8 Profile.ownedClasses → Task A.3
- §9 ClassSelectScreen → Task E.2 + F.1
- §10 Player модель → Task A.1
- §11 Engine изменения → Phase B fully
- §12 PlayerView → Task D.1
- §13 Бот → Phase C
- §14 HUD → Task E.3 + E.4

✅ **No placeholders:** все задачи имеют конкретный код или конкретные команды.

✅ **Type consistency:** `defaultClassState`, `initialPlayerForClass`, `classMaxLives`, `classStartingInventory`, `resolveDamage`, `botDecide`, `mountClassSelectScreen` — везде согласованные сигнатуры.

⚠️ **Намеренные упрощения:**
- E.3 (PlayerListHud) и E.4 (ItemBarHud) — частичная имплементация описана, точные правки зависят от текущей структуры файлов (которые написал Codex в Phase 1). Имплементер может потребоваться адаптировать.
- F.1 (Controller) — описан flow, но точные изменения зависят от текущего контроллера. Имплементер должен внимательно интегрировать.

## Риски

1. **Дробные жизни ломают существующие тесты** — нужно пройтись по `lives === 0`, `lives === 4` ассертам и убедиться что они работают со 0.5
2. **HUD half-heart рендеринг** — emoji 💔 может не отображаться единообразно. План Б: SVG sprite
3. **Бот выбирает случайный класс** — в e2e тесте бот может оказаться Богом и выиграть быстрее ожидаемого. Тест должен быть устойчивым к выбору бота

---

**Конец плана.**
